# 🌐 Ali Efe Deniz - Kişisel Web Sitesi

Bu proje, HTML5 ve CSS kullanılarak oluşturulmuş **çok sayfalı bir kişisel web sitesi** örneğidir.
Proje kapsamında HTML etiketlerinin semantik yapısı, temel CSS stilleri ve çok sayfalı navigasyon yapısı uygulanmıştır.

---

## 📁 Proje Yapısı

├── index.html # Ana sayfa
├── about.html # Hakkında sayfası
├── education.html # Eğitimler sayfası
├── contact.html # İletişim sayfası
├── styles.css # Ortak stil dosyası
├── favicon/ # Favicon ikonları
└── img/ # Görseller

---

## 🧠 Proje Amacı

Bu web sitesi, temel **HTML5 yapısını**, **navigasyon menüsü** oluşturmayı ve **çok sayfalı web sitesi mantığını** öğretmek amacıyla geliştirilmiştir.
Ayrıca:

-   Her sayfada tutarlı bir tema ve renk düzeni oluşturulmuştur 🎨
-   Semantik HTML etiketleri kullanılmıştır (`<header>`, `<nav>`, `<section>`, `<footer>`)
-   Tüm sayfalarda erişilebilirlik için `alt` ve `label` öznitelikleri tanımlanmıştır.

---

## 🚀 Özellikler

-   4 sayfadan oluşan yapısal web sitesi
-   Sabit (sticky) navigasyon menüsü
-   Responsive ve okunabilir sayfa düzeni
-   Form elemanları (ad, e-posta, mesaj)
-   CSS ile temel tasarım estetiği
-   Favicon ve görsel optimizasyonu
-   GitHub Pages üzerinden deploy edilebilir yapı

---

## 🧭 Sayfa İçerikleri

### 🏠 Ana Sayfa (`index.html`)

-   Selamlama (`<h1>`)
-   Tanıtım paragrafı (`<p>`)
-   Görsel (`<img>`)
-   Network iletişim araçları (`<footer>`)

### 👤 Hakkında (`about.html`)

-   Proje sahibi hakkında bilgiler (`<h2>`)
-   Kısa biyografi ve tarihçeler (`<p>`, `<ol>`)

### Eğitimler (`education.html`)

-   Alınan eğitimlerin bilgiler(`<h2>`, `<p>`)
-   Eğitimlerin sıralı listesi (`<ol>`)

### 💬 İletişim (`contact.html`)

-   Adres, e-posta, telefon bilgileri (`<ul>`)
-   İletişim formu (`<form>`, `<input>`, `<textarea>`)

---

## 💡 Kullanılan Teknolojiler

| Teknoloji        | Açıklama                                  |
| :--------------- | :---------------------------------------- |
| **HTML5**        | Sayfa yapısı, etiketler, semantik içerik  |
| **CSS3**         | Görsel düzen, renkler, responsive tasarım |
| **Git / GitHub** | Versiyon kontrolü ve yayınlama            |
| **GitHub Pages** | Web sitesini canlı olarak yayınlamak için |

---

    ```
